name: poll

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:
    inputs:
      bootstrap:
        description: "Bootstrap mode (no notifications, mark all current jobs as already-notified)"
        type: boolean
        default: false
      replay_since:
        description: "One-shot replay: post to Discord every accepted job posted on/after this date (YYYY-MM-DD). Leave blank for normal run."
        type: string
        default: ""

permissions:
  contents: write   # required to commit jobs.db back

concurrency:
  group: poll
  cancel-in-progress: false   # queue runs; never race two crons on the SQLite file

jobs:
  poll:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Fetch + filter + notify
        env:
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
          DISCORD_CHANNEL_ID: ${{ secrets.DISCORD_CHANNEL_ID }}
        run: |
          if [[ "${{ inputs.bootstrap }}" == "true" ]]; then
            python -m src.main --bootstrap
          elif [[ -n "${{ inputs.replay_since }}" ]]; then
            python -m src.main --replay-since "${{ inputs.replay_since }}"
          else
            python -m src.main
          fi

      - name: Sync reactions
        if: ${{ inputs.bootstrap != 'true' && inputs.replay_since == '' }}
        env:
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
          DISCORD_CHANNEL_ID: ${{ secrets.DISCORD_CHANNEL_ID }}
        run: python -m src.sync_reactions

      - name: Commit state
        run: |
          git config user.name "job-tracker-bot"
          git config user.email "bot@users.noreply.github.com"
          git add jobs.db
          if git diff --staged --quiet; then
            echo "no state changes to commit"
          else
            git commit -m "state: $(date -u +%Y-%m-%dT%H:%MZ) [skip ci]"
            git push
          fi
